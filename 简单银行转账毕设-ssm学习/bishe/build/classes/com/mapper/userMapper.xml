<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.UserMapper">
    <!-- 解决表名与字段不匹配 -->
    <resultMap type="User" id="userResultMap">
        <id property="uid" column="uid"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
    </resultMap>
    
    <select id="findAllUser" resultMap="userResultMap">
        select * from users
    </select>
    <select id="findOneUser" resultMap="userResultMap" parameterType="User">
        select * from users where username=#{username} and password=#{password}
    </select>
    <insert id="insertUser" parameterType="User">
    insert into users (username,password,email) values(
    #{username},#{password},#{email})
    </insert>
    
    <update id="updateUserPwdByUserId" parameterType="User">
    update users set password = #{password} where uid = #{uid}
    </update> 
    <select id="findOneUserByUserId" resultMap="userResultMap" parameterType="User">
        select * from users where uid=#{uid} 
    </select>
    <delete id="delUserByUserId" parameterType="java.lang.Integer" >
    delete from users
    where uid = #{uid}
  </delete>
</mapper>